import { ClearStringPipe } from './clear-string.pipe';

describe('ClearStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ClearStringPipe();
    expect(pipe).toBeTruthy();
  });
});
